<%- include('partials/header', {titulo: titulo}) %>
<a class="btn btn-success mb-3" href="/vuelos/nuevo">Agregar vuelo</a>
<table class="table table-striped table-dark">
  <thead><tr><th>Código</th><th>Origen</th><th>Destino</th><th>Salida</th><th>Asientos</th><th>Acciones</th></tr></thead>
  <tbody>
    <% flights.forEach(function(f){ %>
      <tr>
        <td><%= f.code %></td>
        <td><%= f.origin %></td>
        <td><%= f.destination %></td>
        <td><%= f.departure %></td>
        <td><%= f.seats_available %> / <%= f.seats_total %></td>
        <td>
          <a class="btn btn-sm btn-warning" href="/vuelos/editar/<%= f.id %>">Editar</a>
          <a class="btn btn-sm btn-danger" href="/vuelos/borrar/<%= f.id %>">Borrar</a>
        </td>
      </tr>
    <% }) %>
  </tbody>
</table>
<%- include('partials/footer') %>
